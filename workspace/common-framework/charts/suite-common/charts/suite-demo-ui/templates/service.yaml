apiVersion: v1
kind: Service
metadata:
  name: {{ template "suite-demo-ui.name" . }}
  labels:
    app: {{ template "suite-demo-ui.name" . }}
    chart: {{ template "suite-demo-ui.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
    app: {{ template "suite-demo-ui.name" . }}
    release: {{ .Release.Name }}
