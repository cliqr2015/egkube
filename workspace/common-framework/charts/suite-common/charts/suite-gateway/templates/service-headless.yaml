apiVersion: v1
kind: Service
metadata:
  name: {{ template "suite-gateway.name" . }}
  labels:
    app: {{ template "suite-gateway.name" . }}
    chart: {{ template "suite-gateway.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.httpsPort }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ template "suite-gateway.name" . }}
    release: {{ .Release.Name }}
