apiVersion: v1
kind: Service
metadata:
  name: {{ template "suite-cryptoservice.name" . }}
  labels:
    app: {{ template "suite-cryptoservice.name" . }}
    chart: {{ template "suite-cryptoservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      name: http
    - port: {{ .Values.service.httpsPort }}
      targetPort: https
      name: https
  selector:
    app: {{ template "suite-cryptoservice.name" . }}
    release: {{ .Release.Name }}
