apiVersion: v1
kind: Service
metadata:
  name: {{ template "suite-license.name" . }}
  labels:
    app: {{ template "suite-license.name" . }}
    chart: {{ template "suite-license.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      name: http
    - port: {{ .Values.service.httpsPort }}
      targetPort: https
      name: https
  selector:
    app: {{ template "suite-license.name" . }}
    release: {{ .Release.Name }}
