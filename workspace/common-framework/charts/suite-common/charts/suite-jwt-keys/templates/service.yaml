apiVersion: v1
kind: Service
metadata:
  name: {{ template "suite-jwt-keys.name" . }}
  labels:
    app: {{ template "suite-jwt-keys.name" . }}
    chart: {{ template "suite-jwt-keys.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.httpsPort }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ template "suite-jwt-keys.name" . }}
    release: {{ .Release.Name }}
