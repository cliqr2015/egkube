apiVersion: v1
kind: Service
metadata:
  name: {{ template "suite-res-mgmt.name" . }}
  labels:
    app: {{ template "suite-res-mgmt.name" . }}
    chart: {{ template "suite-res-mgmt.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      name: http
    - port: {{ .Values.service.httpsPort }}
      targetPort: https
      name: https
  selector:
    app: {{ template "suite-res-mgmt.name" . }}
    release: {{ .Release.Name }}
