apiVersion: v1
kind: Service
metadata:
  name: {{ template "suite-password.name" . }}
  labels:
    app: {{ template "suite-password.name" . }}
    chart: {{ template "suite-password.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      name: http
    - port: {{ .Values.service.httpsPort }}
      targetPort: https
      name: https
  selector:
    app: {{ template "suite-password.name" . }}
    release: {{ .Release.Name }}
