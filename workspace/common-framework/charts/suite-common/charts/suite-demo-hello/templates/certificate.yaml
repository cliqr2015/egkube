apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: suite-demo-hello-tls
spec:
  secretName: suite-demo-hello-tls
  issuerRef:
    name: ca-issuer
    # We can reference ClusterIssuers by changing the kind here.
    # The default value is Issuer (i.e. a locally namespaced Issuer)
    kind: Issuer
  commonName: suite-demo-hello
  dnsNames:
  - suite-demo-hello