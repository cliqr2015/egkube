apiVersion: v1
kind: Service
metadata:
  name: {{ template "suite-logs.name" . }}
  labels:
    app: {{ template "suite-logs.name" . }}
    chart: {{ template "suite-logs.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      name: http
    - port: {{ .Values.service.httpsPort }}
      targetPort: https
      name: https
  selector:
    app: {{ template "suite-logs.name" . }}
    release: {{ .Release.Name }}
